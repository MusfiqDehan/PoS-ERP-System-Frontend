FROM node:25.9.0-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 3002

# Source code is bind-mounted from the host for hot reload
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0", "-p", "3002"]
