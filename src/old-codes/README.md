# old-codes

Legacy monolith entry points and re-export shims from the component modularization refactor.

These files are **not used by active routes**. Current pages import modular components directly from `src/components/<Domain>/` and compose them in `src/app/.../page.tsx`.

Preserved here for reference only. Safe to delete once no longer needed.
