// Extends Vitest's `expect` with DOM matchers like `toBeInTheDocument()`.
// Loaded automatically before each test file (see vitest.config.ts -> setupFiles).
import "@testing-library/jest-dom/vitest";
